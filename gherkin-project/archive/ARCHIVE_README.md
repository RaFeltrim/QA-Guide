Arquivos arquivados provenientes de `features/` após reorganização.

Regra: estes arquivos foram arquivados após serem movidos para `docs/cnpj/`.
Mantê-los por 30 dias antes da exclusão definitiva.

Como foram criados: para cada arquivo `.md` original em `features/` foi gerado um arquivo com um cabeçalho explicando a origem e um link relativo para o novo local em `docs/cnpj/`.

Lista de arquivos arquiváveis e instruções:
- Copie ou revise o conteúdo em `docs/cnpj/` antes de excluir definitivamente qualquer item.
- Para restaurar, mova o arquivo de volta para `features/` e remova o cabeçalho ARCHIVE.
