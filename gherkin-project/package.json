{
  "name": "gherkin-project",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "cucumber-js --format progress --format json:reports/cucumber.json",
    "test:smoke": "cucumber-js --tags @smoke --format progress --format json:reports/cucumber-smoke.json"
  },
  "devDependencies": {
    "@cucumber/cucumber": "^9.0.0",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.5",
    "@types/node": "^18.11.18",
    "chai": "^4.3.7"
  }
}
